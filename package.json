{
  "name": "novel-site-d1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Building static site...' && cp index.html public/ 2>/dev/null || true",
    "dev": "echo 'Static site - no dev server needed'"
  },
  "devDependencies": {}
}